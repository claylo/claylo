# ah-ah-ah

Offline token counting for Claude and OpenAI models. Two backends, one trait, zero network calls. Count tokens locally with a 376-line Rust library that embeds 38,360 API-verified Claude token strings and wraps OpenAI's o200k_base BPE encoder.
